def commomwords(sen1,sen2):
    sen1=set(sent1)
    sen2=set(sent2)
    common=list(sen1.interesection(sen2))
    return common
def removecommonwords(sen1,sen2):
    sentence1=list(sent1.split())
    sentence2=list(sent2.split())
    commonlist=commonwords(sentence1,sentence2)
    word=0
    for i in range(len(sentence1)):
        if sentence[word] in commomlist:
            sentence1.pop(word)
            word=word-1
        word=word+1
    word=0
    for i in range(len(sentence2)):
        if sentence2[word] in commomlist:
            sentence2.pop(word)
            word=word-1
        word=word+1
    print(*sentence1)
    print(*sentence2)
s1="sky is blue in colour"
s2="raj likes sky blue colour"
